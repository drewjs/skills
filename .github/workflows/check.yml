name: check

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check-skills:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: scripts/check-skills.sh
